# سياسة الأمان — Dyar v10 Security Policy

نحمي زبائننا وتجّارنا وسائقينا بطبقات دفاع متعددة. هذه الوثيقة تحدد
سياسة الإبلاغ والضمانات المعمارية.

## الإبلاغ عن ثغرة (Responsible Disclosure)
- راسلنا فورًا: **security@dyar.app** (لا تفتح Issue عامًا لثغرة).
- زمن الاستجابة المستهدف: إقرار خلال **48 ساعة**، تقييم خلال **7 أيام**،
  وإصلاح الحرج خلال **14 يومًا**.
- نرحب بالبحث الأخلاقي على حسابات تجريبية فقط؛ يُحظر المساس ببيانات
  مستخدمين حقيقيين أو تعطيل الخدمة.

## الإصدارات المدعومة
| الإصدار | مدعوم |
|---|---|
| main (v10) | ✅ |
| ما قبل v10 | ❌ |

## الضمانات المعمارية (Defense in Depth)
1. **انعدام الثقة بالعميل**: التسعير والعمولات وحالات الطلب والأرباح
   تُحتسب وتُفرض حصريًا في Cloud Functions (آلات حالات + transactions).
2. **Firestore Rules** denial-by-default بأدوار Custom Claims
   (admin/staff/partner/driver/customer) — **مُختبَرة آليًا** في
   `backend/tests/rules.test.mjs` (تصعيد الدور، تلاعب المحفظة/التسعير،
   اعتماد ذاتي للمتاجر... كلها مرفوضة).
3. **OTP متعدد الطبقات**: دخول بالهاتف (Firebase Phone Auth) + **رمز
   تسليم OTP للطرود** (حماية ديار) + OTP خيار في مواقع White-label.
4. **قفل التطبيق Face ID/بصمة** (`AppLockGate`) في التطبيقات الثلاثة.
5. **سجلات أمنية وتدقيق**: كل تسجيل دخول (جهاز/منصة/وقت) وكل تحول حالة
   وكل تسوية مالية → مجموعة `logs` تظهر في لوحة التحكم.
6. **حد المعدل**: إغراق الطلبات مرفوض خادميًا (resource-exhausted).
7. **الأسرار**: لا مفاتيح في الكود إطلاقًا — Functions Secrets و`.env`
   خارج Git (مفروض بالمراجعة وبـ gitignore).
8. **الدفع**: تأكيد عبر Webhooks موقعة (Stripe) / مفتاح بوابة
   (EasycardNG)، والمبالغ من الخادم فقط.
9. **App Check + بصمات التطبيقات**: تُفعَّل مع مفاتيح الإنتاج
   (راجع `docs/KEYS-RUNBOOK.md`).

## نطاق خارج التغطية
هندسة اجتماعية، هجمات فيزيائية، حسابات مستخدِمين أساؤوا حفظ بياناتهم.
