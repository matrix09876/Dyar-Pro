# 🎨 مكتبة برومبتات DYAR PRO V10 — برومبت لكل شريحة وفئة وقسم
fileKey: `FtF2s8zxdAMS2nDJeUFYPS` · كل برومبت = مواصفة توليد/تحديث إطار
مطابقة للكود حرفيًا. الاستخدام: MASTER + برومبت الإطار → أي وكيل/مصمم
يعيد إنتاج الشاشة بدقة تنافسية.

**التغطية الكاملة (66 شريحة)**: User ‏U1–U19 · Driver ‏D1–D11 ·
Partner ‏P1–P8 · Dashboard ‏A1–A22 · Web ‏W1–W2 · Super-App ‏C1–C8
(فئات + حجوزات قرب-مني + استشارات + تسوق + B2B).

---

## 🧬 MASTER PROMPT (يسبق كل برومبت إطار)
> صمّم إطار iPhone ‏390×844 (أو Desktop ‏1440×900 للوحة) لتطبيق ديار —
> سوبر-آب توصيل ينافس Wolt/Talabat/HAAT/Careem/Noon في الجليل.
> **Tokens إلزامية**: Primary `#F4691E`، تدرّج CTA `#FF8A3D→#E04E12`،
> Ink `#1A1A1F`، Muted `#6B7280`، خلفية `#F6F7FB`، Success `#16A34A`،
> داكن `#15151A`/بطاقات `#1E1E25`. خط **Noto Sans Arabic**
> (Black للعناوين/Bold للأزرار/Regular للنص). زوايا 16 (بطاقات) /
> 24 (هيرو/شيتات) / 999 (شارات). CTA رئيسي **54px** بتدرّج وظل
> `brand 40%, blur 14, y6`؛ أهداف لمس ≥44px. **RTL أولاً**: البداية يمين،
> أيقونة السهم للرجوع ←، الأرقام/العملة LTR داخل النص (₪ قبل الرقم).
> **حالات CTA**: hover تفتيح 6% · pressed scale 0.97 + تعتيم 8% ·
> disabled `#E5E7EB/#9CA3AF`. فوق الإطار شارة دائرية 44px برتقالية برقم
> + عنوان، وتحته شريحة CTA-spec (خلفية `#FFF7ED`، إطار برتقالي، r14)
> تسرد: كل عنصر قابل للنقر → فعله → وجهته → حالاته.
> **3 ثوانٍ**: الشاشة تجيب فورًا: ما هذا؟ / لماذا يهمني؟ / ماذا أفعل؟

---

## 01 · Dyar User

**U1 — Splash/Onboarding** (`main.dart`+قرار P0#6)
> خلفية تدرّج البراند كاملًا، لوغو «د» أبيض داخل دائرة 96px بظل،
> سطر "كل ما تحتاجه في مكان واحد" + سلايد واحدة فقط: 3 نقاط قيمة
> (4 نقرات للطلب · 3 لغات · حماية ديار OTP)، CTA "ابدأ الآن" + هدية
> ₪30 chip ذهبي. Careem-grade: حركة دخول للوغو موثقة بالـspec.

**U2 — Home** (`home_screen.dart`)
> ترويسة متدرجة 200px بزوايا سفلية 34: تحية "👋 ديار" Black 26 +
> سطر الموقع، زرّا جرس و🛍️/💼 دائريان (أبيض 18%). بحث عائم أبيض
> 54px يطفو -26px بظل blur22. صف Stories 64px بحلقة تدرّج ذهبي→برتقالي→وردي
> وصور حقيقية. بانرات 290×120 (أخضر توصيل مجاني / بنفسجي DYAR10 /
> أزرق تاكسي). شبكة خدمات 62px بتدرج مميز لكل فئة وحالة اختيار
> (إطار أبيض 3px + ظل أقوى). بطاقتا تاكسي (داكن) وطرود (كهرماني)
> 84px. عنوان "الأشهر بالقرب منك 🔥" + بطاقات متاجر: غلاف 150px،
> شارة تقييم عائمة بيضاء، شارة مفتوح خضراء، ♥ دائري أبيض أسفل-نهاية،
> رقاقتا وقت/توصيل، شيفرون. Noon-grade density بلا فوضى.

**U3 — Store** (`store_screen.dart` + `menu_templates.dart`)
> غلاف 240px بتظليل علوي/سفلي، زر رجوع أبيض 42px. بطاقة معلومات
> تطفو -56px: الاسم Black20 + شارة تقييم `#FFF7E6`، الوصف، رقاقات
> وقت/توصيل، زر "حجز طاولة/موعد" outline برتقالي 46px (مطعم/خدمة).
> شريط أقسام لاصق (chips تقفز للقسم). أصناف: بطاقة بيضاء r20،
> صورة 78px r14، اسم Bold14.5 + وصف سطرين + سعر براند Black15،
> زر + متدرّج 42px → عدّاد − qty +. صنف غير متوفر: Opacity 55% +
> شارة "غير متوفر". **هوية المتجر**: طبّق MenuBrand (elegant فحمي-ذهبي /
> street ناري / fresh أخضر / pharma أزرق / boutique بنفسجي) على
> الترويسة والشارات وزر الإضافة وشريط السلة.

**U4 — Item Sheet** (`item_sheet.dart`)
> شيت r28 من الأسفل: صورة 190px + زر ✕ دائري، الاسم Black19 + الوصف،
> "الإضافات" مع CheckboxList (سعر +₪ LTR يمين كل خيار)، أسفله صف:
> عدّاد رمادي r14 + زر ممتد "أضف إلى السلة | ₪64.00" يحدّث المجموع
> حيًا مع كل اختيار. Talabat-grade: السعر الحي هو الـhero.

**U5 — Checkout** (`checkout_screen.dart`)
> ترتيب الإقناع: ملخص الأصناف ببطاقة → chips توصيل/استلام-وفّر →
> بطاقة العنوان (📍 + تغيير) → بقشيش 0/2/5/10 ChoiceChips → حقل
> كوبون بأيقونة 🎁 → ملاحظات 📍 → الآن/جدولة chips → **VISA/CASH/BIT**
> بطاقات اختيار بإطار برتقالي 2px وعلامة ✓ → CTA "إتمام الطلب | ₪"
> يتحول لـ"الحد الأدنى ₪40" معطّلًا تحت الحد. Wolt-grade: شاشة واحدة، صفر تشتيت.

**U6 — Tracking** (`tracking_screen.dart`)
> هيرو ETA متدرّج r24: #كود + إجمالي، "يصل خلال" White70، عدّاد
> **mm:ss** Black34 يتكتك، شريط تقدّم أبيض 8px. تحته خط زمني عمودي
> 6 مراحل (دائرة 28px مكتملة براند/رمادية + خط واصل) ثم بطاقة موقع
> السائق الحي (نقطة بث خضراء). عند delivered: بطاقة تقييم 5 نجوم.

**U7 — Booking Sheet** (`booking_sheet.dart`)
> chips أيام (اليوم/غدًا/بعد غد) → شبكة أوقات 30د (الماضي معطّل) →
> stepper أشخاص → ملاحظات → سويتش "تذكير قبل 30د" → CTA
> "احجز الآن — ₪5.00/مجانًا". Booking-grade وضوح.

**U8 — Marketplace** (`marketplace_screen.dart`)
> تبويبا تصفّح/منتجاتي، بحث، شبكة 2×2 بطاقات (صورة مربعة، سعر براند،
> مدينة/فئة chips)، حالات منتجاتي StatusChip، FAB "أضف منتجًا" →
> شيت (عنوان/وصف/سعر/فئة chips/صورة). Noon-grade C2C.

**U9 — Services Hub** (`services_screen.dart`)
> ترويسة متدرجة + شبكة 3×N للمهن الـ14 (emoji 28 + تدرّج مميز لكل
> مهنة + التسمية)؛ Providers: بطاقات بزر "حجز موعد" متدرّج + "اتصال" أخضر.

**U10 — Jobs** (`jobs_screen.dart`)
> تبويبا الوظائف/طلباتي؛ بطاقة وظيفة (أيقونة نوع، عنوان Bold،
> راتب/نوع chips) → شيت تفاصيل + نموذج (اسم/هاتف/CV متعدد الأسطر)
> CTA "قدّم الآن"؛ طلباتي بـ StatusChip ‏new/shortlisted/hired.

**U11 — Profile** (`profile_screen.dart`)
> بطاقة محفظة (رصيد حي MoneyText) ثم تايلات: عناويني/إشعارات/
> ادعُ-واربح (رمز DY… قابل للتحديد)/استرداد هدية، ثم لغات
> SegmentedButton ثلاثي + داكن سويتش + خروج أحمر.

**U12 — Dark Home**
> نفس U2 بأسطح `#15151A/#1E1E25`، نصوص بيضاء/White70، التدرجات
> كما هي (البراند لا يخفت في الداكن).

**U13 — Login/OTP** (`login_screen.dart`)
> لوغو «د» متدرّج 88px، حقل هاتف +972 LTR بإطار يركّز براند، CTA
> "أرسل الرمز" → 6 صناديق OTP ‏48×56 + عدّاد إعادة إرسال 0:59 +
> "تغيير الرقم". سطر حماية: "🛡️ محمي بحماية ديار OTP".

**U14 — Addresses** (`addresses_screen.dart`)
> قائمة بطاقات عنوان: 📍 + label Bold (منزل/عمل) + سطر العنوان Muted +
> 🗑️ حذف. FAB ممتد متدرّج "أضف عنوانًا" → حوار (label + سطر العنوان +
> إلغاء/حفظ). حالة فارغة ودودة "أضف عنوانك الأول 🏠".

**U15 — Notifications** (`notifications_screen.dart`)
> صفوف: أيقونة دائرية بصندوق `#FFF7ED` + title Bold14 + body Muted
> سطرين + وقت نسبي؛ غير المقروء بخلفية `#FFF7ED` خفيفة + نقطة براند.

**U16 — My Bookings** (`bookings_screen.dart`)
> بطاقة حجز: اسم المتجر Bold + 📅 الموعد + 👥 الأشخاص، StatusChip
> ملوّن (pending كهرماني/confirmed أخضر/seated أزرق)، رسوم MoneyText،
> ملاحظات، زر "إلغاء" outline أحمر للقادمة فقط.

**U17 — Taxi Request** (`taxi_screen.dart`)
> AppBar "تاكسي" + **زر SOS أحمر** أعلى-نهاية (حماية ديار). بطاقتا
> 🟢 انطلاق / 🔴 وجهة. 3 بطاقات فئة (قياسي ₪12 / راحة ₪18 / عائلي XL
> ₪24) اختيار بإطار براند 2px + أيقونة سيارة/باص، السعر من الخادم.
> CTA "اطلب مشوارًا 🚕" → شاشة تتبع المشوار (حالة حية + بيانات السائق).

**U18 — Send Parcel** (`parcel_screen.dart`)
> نموذج بطاقتين: المرسِل (اسم/هاتف) + المستلِم (اسم/هاتف) + حقل وزن
> kg stepper → CTA "أرسل الطرد 📦 | ₪السعر" → شاشة نجاح: **رمز تسليم
> OTP ‏4 خانات ضخم Black36 ببطاقة متدرجة** + "أعطِ الرمز للمستلم —
> السائق لا يُسلِّم بدونه 🛡️" + الإجمالي.

**U19 — My Orders** (`orders_screen.dart`)
> قائمة طلباتي: بطاقة (#كود Bold + متجر + تاريخ + StatusBadge +
> إجمالي MoneyText) → نقرة تفتح U6 Tracking. فلتر chips نشطة/مكتملة.
> زر "أعد الطلب 🔄" على المكتملة (ولاء Wolt-grade).

## 02 · Dyar Driver

**D1 — OTP Login** (`login_screen.dart`)
> لوغو مربع متدرّج 88px «د»، حقل +972 LTR ثم حقل 6 خانات، CTA
> متابعة/دخول 54px. Careem-grade بساطة.

**D2 — KYC** (`register_screen.dart`)
> 3 بطاقات مركبة (🚗/🏍️/🚲) اختيار بإطار براند 2px، حقول لوحة/رخصة/
> هوية LTR، CTA "متابعة ✓" + سطر "طلبك قيد المراجعة" Muted.

**D3 — Tasks Hub** (`home_screen.dart`)
> ترويسة: "ديار Driver" + ActiveToggle أخضر. بانر تعرفة ديناميكية
> متدرّج أخضر. بانرات مهام نشطة متدرجة برتقالية (🍔/🚕/📦) قابلة
> للنقر. 3 شرائح أنواع (emoji + عدّاد Black، المحدد متدرّج بظل).
> بطاقات متاحة: كود/وزن/مسافة يمينًا، **نصيب السائق أخضر** يسارًا،
> سطر تفاصيل Muted، CTA "استلام المهمة 🤘". Wolt-Courier-grade.

**D4 — Active Task** (`active_task_screen.dart`)
> بطاقة الطلب (كود+حالة، الأصناف، الإجمالي، "أرباحي" أخضر Black) →
> بطاقة عنوان بملاحة → CTA المرحلة التالية (استلمت→في الطريق→تم ✅).

**D5 — Active Parcel** (`active_parcel_screen.dart`)
> حالة+مبلغ → بطاقتا مرسِل/مستلِم مع 📞 أخضر → عند النقل: عنوان
> "رمز التسليم" + **4 صناديق OTP ‏64×72 Black28 بمسافة 12** + CTA
> "تم التسليم ✅". حماية-ديار هي البطل.

**D6 — Active Ride** (`active_ride_screen.dart`)
> بطاقة داكنة `#111827→#374151` r22: StatusChip + سعر أبيض Black20،
> 🟢 انطلاق / 🔴 وجهة، سطر كم/ذروة White70 → ملاحة → CTA مرحلة
> ("وصلت 📍"/"بدأ 🚕"/"اكتمل ✅").

**D7 — Earnings** (`earnings_screen.dart`)
> 3 تايلات (اليوم🌅/أسبوع📅/إجمالي👛) أيقونة بصندوق `#FFE6D5` +
> مبلغ Black18 → **CTA "سحب فوري — حتى 80%"** → قائمة حركات
> (emoji نوع + type + مبلغ أخضر).

**D8 — Support AI** (`support_screen.dart`)
> فقاعاتي متدرجة يمين/بوت بيضاء يسار بشارة "Dyar Bot 🤖" خضراء،
> صف ردود سريعة ActionChips، إدخال + زر إرسال متدرّج 48px.

**D9 — App Lock** (`app_lock.dart`)
> خلفية `#15151A`، 🔒 56، "ديار مقفل" أبيض Bold18، زر
> "فتح بالبصمة / Face ID".

**D10 — History** (`history_screen.dart`)
> عنوان "السجل" titleLarge، قائمة مهام مُنجزة: بطاقة (#كود Bold +
> تاريخ + نوع emoji 🍔/📦/🚕) ونصيب السائق **أخضر MoneyText** نهاية
> الصف. ملخص أعلى: "أنجزت N مهمة هذا الأسبوع 💪" (تحفيز).

**D11 — Profile & Docs** (`profile_screen.dart`+`register_screen.dart`)
> بطاقة المركبة (نوع 🚗 + لوحة LTR + StatusChip approved أخضر)،
> تايلات: وثائقي (رخصة/هوية — تحديث updateDocuments) / الدعم /
> لغات SegmentedButton / داكن / قفل Face ID سويتش / خروج أحمر.

## 03 · Dyar Partner
**P1 Login** بريد/كلمة + CTA. **P2 Orders** (`orders_screen.dart`):
هيدر اسم المتجر + ActiveToggle، بطاقة طلب: كود/حالة، الأصناف،
نقدًا·cash + إجمالي، صف أزرار قبول أخضر/رفض أحمر 46px (ثم تحضير→جاهز).
**P3 Bookings**: بطاقات بموعد Bold + 👥 + أزرار تأكيد/إجلاس/إكمال/
لم-يحضر. **P4 Menu** (`menu_screen.dart`): صورة 56، سعر براند قابل
للنقر (حوار سعر)، Switch توفر أخضر؛ المعطّل رمادي. **P5 Hours**:
7 صفوف (اسم اليوم 74px + Switch + chips فتح/إغلاق `#FFE6D5`) +
حفظ. **P6 More**: تقييم⭐ / ساعات / عروض / **رصيد متدرّج** /
لغات / داكن / خروج.

**P7 — Promotions** (`promotions_screen.dart`)
> قائمة عروض: دائرة نسبة **‎-20%‎** بصندوق `#FFE6D5` Black + العنوان
> Bold + Switch تفعيل أخضر + 🗑️. FAB ممتد "أضف عرضًا" → حوار
> (عنوان + نسبة خصم + حفظ). حالة فارغة: "العروض تضاعف مبيعاتك 📈".

**P8 — Balance** (`balance_screen.dart`)
> بطاقة هيرو متدرجة r24: "الصافي" White70 + المبلغ Black30 أبيض
> (MoneyText net). تحتها حركات: صف (type chip + تاريخ) والمبلغ
> MoneyText أخضر/أحمر حسب الإشارة. سطر "التسوية الشهرية تلقائية 🤖".

## 04 · Dashboard (Desktop 1440)
**A1 Login** بطاقة على تدرّج. **A2 Overview**: سايدبار يمين 256px
(20 بندًا، النشط `#FFF7ED/#C2410C`)، 4 KPI (أيقونة بصندوق ملوّن +
رقم Black24)، Area chart براند بتعبئة متدرجة، جدول حي بشارات.
**A3 Orders**: مفتاحا Dynamic fare/Assignment، 9 فلاتر chips،
جدول، Drawer 448px: StatusBadge كبير + **Timeline** (نقطة براند +
شارة + وقت + 🤖/uid) + أصناف + تعيين سائق. **A4 City Settings**:
بطاقتا ساعات/ذروة + شرائح مناطق ٤ + شبكة فئات الرؤية 11 checkbox.
**A5 Marketplace**: تبويبات بعدادات + بطاقات منتج + حقل عمولة.
**A6 Settings**: نموذج عام + **جدول قواعد العمولة** (نطاق/مطابقة/
نسبة/ذروة من-إلى/نسبة ذروة + إضافة/حذف).

**A7 Stores** (`Stores.tsx`): جدول (اسم/نوع/تقييم/سعر/**عمولة%**/
حالة/إجراءات) — شارة حالة ملوّنة، أزرار اعتماد أخضر/تعليق أحمر،
عمولة قابلة للتعديل صفًا بصف.
**A8 Drivers** (`Drivers.tsx`): جدول (ID/مركبة/أونلاين 🟢/أرباح/
تقييم/حالة/إجراءات) + اعتماد/حظر؛ نقطة بث خضراء للمتصل.
**A9 Users** (`Users.tsx`): جدول (اسم/هاتف/بريد/Role chip/محفظة/
حالة) + زر حظر أحمر ⇄ فك حظر براند.
**A10 Rides** (`Rides.tsx`): جدول (تاريخ/Tier/حالة/KM/**Surge ⚡**/
إجمالي) بشارات حالة المشوار.
**A11 Parcels** (`Parcels.tsx`): جدول (تاريخ/من/إلى/KG/حالة/إجمالي)
— شارة in_transit زرقاء وdelivered خضراء.
**A12 Bookings** (`Bookings.tsx`): جدول حجوزات (متجر/موعد/أشخاص/
حالة/رسوم) بشارات pending/confirmed/seated/completed/no_show.
**A13 Marketing** (`Marketing.tsx`): 3 تبويبات **coupons/promotions/
giftcards**، جدول كوبونات (كود/خصم/استخدام/تفعيل بنقرة/🗑️)، زر
"إضافة" + حوار، بطاقات عروض بتبديل نشط، جدول بطاقات هدايا برصيد.
**A14 Finance** (`Finance.tsx`): 4 KPI بشارات أنواع الحركات الملوّنة
ثم جدول (تاريخ/نوع chip/Ref/مبلغ tabular) — التسويات والعمولات.
**A15 Banners** (`Banners.tsx`): فلاتر **placements** (stories/main/
lower/driver/category)، شبكة بانرات بصورة + target chip + 🗑️ +
نموذج إضافة (صورة/عنوان/مكان/هدف).
**A16 Jobs** (`Jobs.tsx`): وظائف + متقدمون (CV متعدد الأسطر) بشارات
new/shortlisted/hired وأزرار ترقية الحالة.
**A17 Support** (`Support.tsx`): جدول محادثات (تاريخ/آخر رسالة/
User/حالة) → فتح خيط الرسائل والرد كـ"فريق ديار".
**A18 Cities** (`Cities.tsx`): جدول مدن (اسم/حالة/إجراءات) + زر
إضافة → كل صف يفتح A4 City Settings (رؤية الفئات لكل مدينة).
**A19 Notes** (`Notes.tsx`): كانبان 3 أعمدة (todo/doing/done) ببطاقات
قابلة للنقل وزر إضافة لكل عمود.
**A20 Training** (`Training.tsx`): Training/FAQ — أقسام (سائق/تاجر/
زبون) ببطاقات سؤال Bold + جواب Muted + إضافة/تحرير.
**A21 Logs** (`Logs.tsx`): 9 تبويبات (all/**security**/business/menu/
users/orders/zones/timings/discounts) + جدول (تاريخ/فئة chip/تفاصيل/
By mono) — تبويب security يُبرز تسجيلات الدخول 🛡️.
**A22 Team** (`Team.tsx`): جدول staff (اسم/Role/بريد/إجراءات) +
صلاحيات staffCan chips + دعوة عضو.

## 05 · dyar.app Web
**W1 Desktop 1440×1900**: هيدر (لوغو، لغات pill، 🌙)، هيرو عنوان
متدرّج نصيًا 64px + وصف + أزرار المتاجر السوداء + CTA تاجر/سائق
outline، شريط قيمة (٤ نقرات · ٣ لغات · حماية ديار)، شبكة 10 خدمات
5×2 على `#FFF7ED`، فوتر داكن. **W2 Mobile 390**: مكدّس، أزرار full-width.

## 06 · Super-App — شريحة لكل فئة وقسم
> ديار **سوبر آب** وليس تطبيق توصيل فقط: طلب + خدمات + استشارات +
> حجوزات + ماركت بلايس + تسوق (ملابس/إلكترونيات) + B2B للتجار.
> كل فئة لها شريحة هبوط وتدفّق حقيقي يلبي احتياج المستخدم.

**C1 — Category Hub (قالب هبوط الفئة)** (`home_screen.dart` services)
> إطار لكل فئة من فئات الرؤية الـ11: 🍔 مطاعم / 🛒 بقالة / 💊 صيدليات /
> 🌹 ورود / 👗 ملابس / 📱 إلكترونيات / 🛠️ خدمات / 🏪 تجار B2B /
> 🛍️ ماركت / 🚕 تاكسي / 📦 طرود. هيرو متدرّج بلون الفئة + بحث داخل
> الفئة + فلاتر chips (الأقرب/الأعلى تقييمًا/توصيل مجاني/مفتوح الآن) +
> شبكة متاجر الفئة. الفئة تظهر حسب رؤية المدينة من اللوحة (A4/A18).

**C2 — Salon Nearby + حجز موعد** (services+booking_sheet+geolocator)
> **التدفّق الحقيقي**: "أظافر بالقرب مني 💅" — بحث بالموقع → قائمة
> صالونات مرتّبة بالمسافة (بطاقة: صورة + اسم + ⭐ + **"0.8 كم" chip
> أخضر** + "أقرب موعد اليوم 14:30" Bold براند) → بروفايل الصالون
> (خدمات بأسعار: أظافر ₪80 / قص ₪60 / صبغة ₪150، معرض صور، تقييمات)
> → U7 Booking Sheet (يوم/ساعة/تذكير) → تأكيد "موعدك محجوز ✅".
> Booking.com-grade: من البحث للحجز في 3 شاشات.

**C3 — Consultation (استشارة)** (`services_screen.dart` providers)
> للمهن الاستشارية (⚖️ محامٍ / 🧮 محاسب / 🩺 طبيب): بروفايل المزوّد
> (صورة + تخصص + سنوات خبرة + ⭐) + chips نوع الاستشارة
> (📞 هاتفية ₪150 / 🏢 بالمكتب ₪250) + مدة 30/60د → حجز موعد U7 +
> دفع الرسوم VISA/CASH/BIT → تذكير قبل 30د تلقائي.

**C4 — Fashion Store (ملابس)** (`menu_templates.dart` boutique)
> قالب MenuBrand **boutique بنفسجي**: غلاف لوك-بوك عريض، شبكة منتجات
> 2×N بصور طويلة 3:4، بطاقة منتج: اسم + سعر + **chips مقاسات
> S/M/L/XL وألوان دوائر** في شيت الصنف (U4 موسّع بالـvariants)،
> شارة "جديد ✨"/"خصم %"، ♥ مفضلة. Noon-Fashion-grade.

**C5 — Electronics Store (إلكترونيات)** (`menu_templates.dart`)
> قالب تقني (أزرق داكن): بطاقة منتج بصورة على أبيض نظيف + اسم +
> **صفوف مواصفات** (شاشة/ذاكرة/بطارية) + شارة "ضمان سنة 🛡️" +
> سعر Black + chips سعة/لون في الشيت + "قارن" outline.

**C6 — B2B تجار الجملة** (دور `merchant` — رؤية بالفئة من اللوحة)
> فئة خاصة تظهر فقط لحسابات التجار: كتالوج جملة (بطاقة: منتج +
> **سعر الجملة** + "حد أدنى 12 قطعة" chip كهرماني + stepper بالكرتونة)
> + "اطلب عرض سعر 📋" CTA ثانوي + شروط دفع (نقدًا/آجل 30 يوم بموافقة
> الإدارة) + حد ائتمان ببطاقة هيرو. التسعير والاعتماد من اللوحة.

**C7 — مكتبة فئات الخدمات (14 مهنة)** (`services_screen.dart`)
> إطار مكتبة: 14 تايل مهنة كمكوّنات (💇 حلاق / 💅 صالون / 🩺 طبيب /
> 🔧 ميكانيكي / ⚡ كهربائي / 🚿 سبّاك / 🎨 دهّان / 🪚 نجّار / ⚖️ محامٍ /
> 🧮 محاسب / 📚 معلّم / 📷 مصوّر / 🧹 تنظيف / 🚜 زراعة)، كلٌّ بتدرّجه
> المميز + emoji 28 + التسمية بالثلاث لغات — مصدر واحد لكل الإطارات.

**C8 — مكتبة MenuBrand (5 هويات متاجر)** (`menu_templates.dart`)
> إطار مكتبة يعرض الهويات الخمس جنبًا إلى جنب (ترويسة + بطاقة صنف +
> زر إضافة + شريط سلة لكل هوية): **elegant** فحمي-ذهبي (مطاعم راقية) /
> **street** ناري (وجبات سريعة) / **fresh** أخضر (بقالة) / **pharma**
> أزرق (صيدليات) / **boutique** بنفسجي (ملابس) — «المنيو هوية المتجر،
> كأننا شركة إعلانات».

---
### قاعدة الترابط (لكل برومبت)
كل زر في الإطار يحمل Reaction ‏ON_CLICK→NAVIGATE داخل صفحته،
والوصلات عبر-الصفحات تُوثَّق بشريحة CROSS-PAGE برتقالية متقطعة.
