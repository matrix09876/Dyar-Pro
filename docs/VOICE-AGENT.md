# 🎙️ الوكيل الصوتي الحي — Dyar Live Support (Talya)

محادثة صوتية فورية طبيعية تمامًا (تسمع وتقاطع وترد كإنسان — ليست
تسجيلات) عبر ElevenLabs Conversational AI. الكود جاهز بالكامل:
- الخادم: `liveSupportUrl` يعيد رابط جلسة موقّعًا (المفتاح لا يغادر الخادم).
- اللوحة: الإعدادات → «🔊 صوت ديار» → حقل **Agent ID**.

> ⚖️ مبدأ ثابت: الصوت طبيعي كبشري بالكامل، لكن إن سأل الزبون
> «إنتِ إنسانة؟» تجيب بصدق أنها مساعدة ديار الذكية. التظاهر بأنها
> موظفة بشرية حقيقية = خداع للعملاء ومخاطرة قانونية — مرفوض.

## ✅ الحالة (12/06/2026): الوكيلة منشورة وتعمل
- **Agent ID**: `agent_8801ktyhd1yve9ybf8bn5m9y27gw` (مربوط كافتراضي
  في اللوحة → الإعدادات → صوت ديار)
- أُنشئت بالبرسونا أدناه حرفيًا + صوت Talya + ‏Stability ‏0.4/0.85،
  واختبار السلوك نجح: «وين طلبي؟» ← طلبت رقم الطلب ولم تخترع بيانات ✓
- **نموذج TTS**: الواجهة الحالية تعرض عائلات (V3/Flash/Multilingual)
  بدل Turbo v2.5. **القرار: Flash** — هو خليفة Turbo بأقل زمن استجابة
  للمكالمات الحية ويُبقي إعدادات الصوت. (Multilingual الحالي يعمل
  لكن بتأخير أعلى؛ V3 يلغي تحكم Stability — مرفوض.)
  ⇒ نقرة واحدة: صفحة الوكيلة → Voice → Model: **Flash**.
- ⚠️ **مفتاح الإنتاج**: ثبت بالفحص أن `get_signed_url` يتطلب صلاحية
  **Conversational AI** على المفتاح (401 بدونها) — عند ضبط
  `ELEVENLABS_API_KEY` للإنتاج أنشئ مفتاحًا يشملها وإلا فستعمل
  أصوات TTS وتفشل جلسات المكالمة الحية.

## 🔗 نقاط الدخول المربوطة فعليًا (12/06)
- **موقع dyar.app**: ودجت المكالمة الرسمي مدمج في `web/index.html` —
  زر عائم «Start a call» ظاهر ويعمل (مثبت بلقطة Playwright).
- **تطبيق الزبون**: حسابي → «🎙️ مكالمة مع تاليا» (kTalyaTalkUrl).
- **تطبيق السائق**: شاشة الدعم → زر سماعة في الشريط العلوي.
- **اللوحة**: الإعدادات → صوت ديار → Agent ID (مضبوط افتراضيًا).

## خطوات الإنشاء (مرة واحدة، ~دقيقتان)

1. **أذونات المفتاح**: ElevenLabs → My Workspace → API Keys →
   مفتاحك → فعّل صلاحية **Conversational AI** (convai write).
   (أو أنشئ الوكيل يدويًا من الواجهة وتجاهل الخطوة.)
2. ElevenLabs → **Conversational AI → New agent**:
   - Name: `Dyar Live Support — Talya`
   - Language: العربية · Voice: **Dyar Support - Talya** (مضافة لحسابك)
   - TTS model: `eleven_turbo_v2_5` (زمن استجابة منخفض)
     · Stability ‏0.4 · Similarity ‏0.85
   - First message:
     `يا هلا فيك بديار! معك تاليا، كيف بقدر أساعدك اليوم؟`
   - System prompt: **انسخ الكتلة أدناه حرفيًا**
3. انسخ **Agent ID** → لوحة ديار → الإعدادات → صوت ديار → الصق → حفظ.
4. (عند النشر) `firebase deploy --only functions:liveSupportUrl`

## System Prompt (انسخه كما هو)

```
أنتِ «تاليا» — مساعدة خدمة العملاء الذكية في ديار (سوبر آب توصيل
وخدمات في الجليل).

أسلوبك: دافئ وعفوي وطبيعي، عربية محكية فلسطينية (يا هلا، تمام،
لحظة بشيكلك، ولا يهمك، صحتين)، جُمَل قصيرة بإيقاع محادثة حقيقية.
إذا حكى معك الزبون بالعبرية أو الإنجليزية انتقلي للغته بطلاقة.
إذا سُئلتِ إن كنتِ إنسانة: أجيبي بلطف وصدق أنك مساعدة ديار الذكية،
وأن فريق الدعم البشري متاح دائمًا من شاشة الدعم.

تعرفين منظومة ديار:
- حالات الطلب: قيد المراجعة ← مقبول ← قيد التحضير ← جاهز ←
  مع المندوب ← في الطريق ← وصل.
- الدفع: فيزا / كاش / Bit. الاسترداد يرجع لمحفظة ديار خلال دقائق.
- التوصيل حسب المنطقة والحد الأدنى يظهر بالتطبيق. كوبون DYAR10 خصم 10%.
- في تاكسي وطرود برمز تسليم OTP وحجوزات مواعيد وسوق بيع وشراء.

قواعد صارمة:
- لا تخترعي بيانات طلب أو أسعار أو أوقات: اطلبي رقم الطلب ووجّهي
  لشاشة التتبع أو أكدي أن الفريق سيتحقق.
- مشكلة معقدة أو زبون منزعج؟ طمنيه وحوّليه لفريق الدعم البشري فورًا.
- لا تطلبي أبدًا رقم بطاقة أو كلمة سر — حماية ديار.
- جواب قصير (جملة لجملتين) ثم اسألي إن كان في شيء آخر تساعدين فيه.
```

## الشخصيات الصوتية الأربع (TTS — دالة `speak`)
أُضيفت لحسابك وجُرّبت حيًا (عينات mp3 سُلّمت 12/06):

| الدور | الصوت | Voice ID |
|---|---|---|
| الدعم الأساسي | Talya — بوت عربي شبيه بالبشر | `rh16DBXwtscjdPFeMBYf` |
| استرداد/اعتذار | Lina — دافئ | `6ZvbKYJmZfL6zVBzLwpV` |
| تنبيهات السائق | Hasawi — حيوي | `kr4VZw8MSZMHE0y2m40n` |
| إعلانات رسمية | Nasser — مؤسسي | `3GnbqfjaW8xI6hRTVx4Y` |

كلها تُدار من اللوحة (config/voice) وتُستبدل بلصق Voice ID آخر.

---

## 🪝 أدوات الوكيل (Tools/Webhooks) — خدمة حقيقية لا كلام

ثلاث دوال جاهزة بالخلفية يستدعيها الوكيل أثناء المكالمة، وكل تذكرة
تظهر **فورًا** في اللوحة → الدعم (مع الشارة الحمراء) ليتابعها فريق
خدمة العملاء. التفعيل عند النشر:
```bash
firebase functions:secrets:set SUPPORT_HOOK_SECRET   # قيمة قوية تختارها
firebase deploy --only functions:hookOrderStatus,functions:hookCreateTicket,functions:hookRefundRequest
```

في ElevenLabs → الوكيل → **Tools → Add tool (Webhook)** أنشئ الثلاثة
(استبدل `<region>-<PROJECT_ID>` برابط مشروعك، وضع السر في الترويسة):

### 1) order_status — حالة وتتبع الطلب
- **Description**: «تجلب حالة طلب ديار الحية ووقت الوصول. استعمليها فور
  ذكر الزبون رقم طلبه. اطلبي آخر 3 أرقام من هاتفه للتحقق.»
- POST `https://<region>-<PROJECT_ID>.cloudfunctions.net/hookOrderStatus`
- Headers: `x-dyar-hook: <SUPPORT_HOOK_SECRET>`
- Body: `orderCode` (string، مطلوب) · `phoneTail` (string، آخر 3 أرقام)
- الرد يتضمن `say` — جملة جاهزة تنطقها تاليا.

### 2) create_ticket — تذكرة لفريق خدمة العملاء
- **Description**: «تفتح تذكرة لفريق الدعم البشري. استعمليها لأي مشكلة
  لا تحلّينها بنفسك أو عندما يطلب الزبون موظفًا. لخّصي المشكلة بدقة.»
- POST `…/hookCreateTicket` + نفس الترويسة
- Body: `topic` (مثل order-issue/driver/app/other) · `summary` (ملخص
  عربي واضح) · `phone` · `orderCode` (اختياريان)

### 3) refund_request — طلب استرداد (بموافقة الموظف)
- **Description**: «تسجّل طلب استرداد لطلب محدد. لا تعدي بإرجاع فوري —
  قولي إنه بعد مراجعة الفريق يرجع للمحفظة.»
- POST `…/hookRefundRequest` + نفس الترويسة
- Body: `orderCode` + `reason` (مطلوبان) · `phone` · `phoneTail`

> 🔒 الأمان: السر في ترويسة `x-dyar-hook` إلزامي (401 بدونه)،
> الردود بلا PII (لا عناوين/هواتف كاملة)، والاسترداد تذكرة موسومة
> `refund` تتطلب موافقة موظف — لا حركة أموال آلية من الوكيل.

## 🎧 دور «خدمة العملاء» في اللوحة
من الفريق → افتح الموظف → زر **«🎧 دور خدمة العملاء»** يمنحه:
`support.manage` + `orders.view` + `orders.manage` — فلا يرى من
اللوحة إلا **الدعم والطلبات**: يرد على التذاكر (ومنها تذاكر تاليا)،
يتتبع الطلبات ويغيّر حالتها لحل المشاكل، **دون** وصول للدفع/التسعير/
المالية/الإعدادات (مفروض في قواعد Firestore نفسها، مختبر 20/20).
