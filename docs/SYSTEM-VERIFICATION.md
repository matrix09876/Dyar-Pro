# 🩺 تقرير التحقق الشامل من النظام (13/06/2026)

فحص كامل بالأدوات عبر كل الطبقات. **لا أعطال ولا فجوات حرجة في الكود
القائم** — النظام سليم. ما يلي: الفحوص الخضراء + قائمة مرتّبة لما نبدأ به.

## ✅ مصفوفة التحقق — كلها خضراء
| الفحص | النتيجة |
|---|---|
| حالة git + صفر علامات تعارض | ✓ نظيف |
| بناء لوحة التحكم (tsc+vite) | ✓ |
| بناء موقع dyar.app | ✓ |
| بناء Cloud Functions (tsc) | ✓ |
| flutter analyze (user/driver/partner) | ✓ **0 أخطاء ×3** |
| اختبارات قواعد Firestore (المحاكي) | ✓ **23/23** |
| اختبارات محرك العمولة | ✓ **10/10** |
| صفحات لوحة بلا route (يتيمة) | ✓ لا يوجد |
| TODO/FIXME/Unimplemented في كود التطبيقات | ✓ لا يوجد |

## 🟡 الفجوات المتبقية (روادماب وجودة — ليست أعطالًا) — رتّبناها للبدء

### 1) اختبارات Flutter — ✅ بدأت (21 اختبارًا الآن)
- **dyar_core (16)**: Store/MenuItem/AppUser/FeatureFlags/DyarStory.fromDoc
  + getters (isHalal/lat/lng/minQty/merchant/points/cover) + geo + dietary.
- **تطبيق الزبون (5)**: منطق السلة والتسعير (CartLine/CartState).
- مُزيّف خفيف لـ DocumentSnapshot (`test/fake_doc.dart`) بلا حِزم mock.
- **التالي**: widget tests لـ checkout/dietary filter + اختبار driver/partner.

### 2) ميزات مصمّمة تنتظر الكود (مرتّبة بالقيمة السوقية)
- **ديار Meals** (بدل وجبات الشركات 10bis/Cibus) — أكبر رافعة بالسوق
  الإسرائيلي (`ISRAELI-COMPETITORS-INTEL.md`).
- **الولاء المتدرّج + Dyar+ بالتاكسي + النقاط الموحّدة** (R1/R2/R3 مصمّمة).
- **الإشعارات الاحترافية** (تصنيف + مقروء + تفضيلات + deep-link).
- **حجوزات Booking-grade** (BK1–10 مصمّمة) — ربطها بالكود وعقد `bookings`.
- **الطابعة + صور الإضافات** (P9/P10 مصمّمة) — ESC/POS من جهاز المتجر.

### 3) متوقّف على مفاتيح/عمليات (موثّق بـ KEYS-RUNBOOK)
- **Google Maps**: خريطة المندوبين الحية + دبوس الموقع بدقة المتر
  (البنية والـUI جاهزان — placeholder الآن).
- **App Check** + **FLAG_SECURE** على شاشة الدفع — قبل النشر.
- **onUserCreate** (Identity Platform) — غير معطِّل (الزبون عبر isOwner،
  المدير عبر claimFirstAdmin).
- تدوير المفاتيح المكشوفة بالدردشة (ElevenLabs/Anthropic) قبل الإطلاق.

## 🎯 التوصية: من أين نبدأ
**أبدأ باختبارات Flutter (#1)** — يحوّل النظام من «يعمل» إلى «متين موثوق»
ويحمي كل ما بُني، ثم **ديار Meals (#2)** لأكبر أثر سوقي. الباقي بالترتيب.

> الحكم: النظام **سليم وأخضر بالكامل ولا مشاكل نشطة**؛ القائمة أعلاه
> تطوير مخطّط لا إصلاح أعطال.
