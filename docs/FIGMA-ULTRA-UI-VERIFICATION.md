# Dyar Ultra UI ↔ Code — تقرير التحقق (Figma Verification)

> الملف المرجعي: **Dyar Ultra UI** — Figma `LI80pm1FvvDMVKiFrwh88i`
> (صفحة `00 · 📦 Master Handoff — START HERE`، node `1317:5`).
> التاريخ: 2026-06-13 · أُجري عبر Figma MCP (whoami: amin abu hikal / info@cyberamin.com).

هذا الملف هو **مصدر الحقيقة للتصميم** (لوحة تسليم/توثيق): نظام التصميم،
قرارات P0، مصفوفة الأيقونات، توحيد الخدمات (16)، والميزتان المتقدمتان. تم
استخراج مواصفاته ومقارنتها بالكود الفعلي.

---

## 1) تطابق رموز التصميم (Design Tokens)

| الرمز | مواصفة Ultra UI | الكود (`DyarTokens` / `DyarTheme`) | الحالة |
|---|---|---|---|
| اللون الأساسي (Brand) | برتقالي ديار | `brand = #F4691E` | ✅ |
| نصف القطر (Radius) | 16px (بطاقات الهاندوف) | `radius = 16.0` | ✅ |
| ارتفاع CTA | 52–56px | `ctaHeight = 54.0` | ✅ |
| أصغر هدف لمس | ≥ 44px | `minTouch = 44.0` | ✅ |
| أصغر خط | ≥ 11px | لا قياسات < 11px في الويدجتس | ✅ |
| الخطوط | Noto Sans Arabic + Inter | `GoogleFonts.notoSansArabicTextTheme` (+fallback عبري/لاتيني) | ✅ |
| الأيقونات | Lucide (stroke 1.8 · 22–24px) | `LucideIcons.*` في كل الواجهات | ✅ |
| ثلاثي اللغة | ar · he · en مع RTL/LTR | `strings.dart` ثلاثي + `langProvider` | ✅ |
| الظل | `rgba(26,20,13,0.05)` | بطاقات بحدّ خفيف 5% أسود | ✅ مكافئ |

**النتيجة:** نظام التصميم منفّذ بأمانة. لا انحراف في الألوان/المقاسات/الخطوط.

---

## 2) مصفوفة الأيقونات (Emoji → Lucide, قرار P0 #7)

الـ chrome (تنقّل/فلاتر/أزرار) Lucide — مطابق. ملاحظة على شريط التنقّل السفلي:

| تبويب Ultra UI | أيقونة المواصفة | الكود (`shell.dart`) | الحالة |
|---|---|---|---|
| الرئيسية | `home` | `LucideIcons.home` | ✅ |
| اكتشف | `compass` | — (مدموج) | ⚠️ مبسّط |
| الطلبات | `receipt-text` | `LucideIcons.receipt` | ✅ مكافئ |
| العروض | `badge-percent` | — (مدموج) | ⚠️ مبسّط |
| حسابي | `user-round` | `LucideIcons.userCircle2` | ✅ مكافئ |

الكود يشحن **3 تبويبات** (رئيسية/طلبات/حسابي) بدل 5 — وهذا **متّسق مع قرار
P0 #1** («توحيد التنقّل: chips أُخفيت — grid الخدمات أساسي»): اكتشف/العروض
دُمجت داخل الرئيسية بدل تبويب مستقل. مقبول، مع فارق تسمية أيقونات طفيف.

---

## 3) تغطية الخدمات الـ16 الموحّدة + الميزتان المتقدمتان

| خدمة Ultra UI | شاشة الكود | الحالة |
|---|---|---|
| الرئيسية | `home_screen.dart` | ✅ |
| الطعام / قائمة المطاعم | `home_screen` (فلاتر) | ✅ |
| صفحة المطعم / المتجر | `store_screen.dart` | ✅ |
| تفاصيل الصنف والإضافات | `item_sheet.dart` | ✅ |
| السلة + الدفع | `checkout_screen.dart` | ✅ |
| منتقي وقت الجدولة | `checkout` (`scheduledFor`) | ✅ |
| تتبع الطلب | `tracking_screen.dart` | ✅ |
| التقييمات والصور | `order_service.rate()` + reviews | ✅ |
| المتاجر/البقالة + المنتجات | `store_screen` | ✅ |
| نتائج البحث الموحّد | `home_screen` (بحث) | ✅ |
| بطاقات الهدايا | `profile.redeemGift` + طلب كهدية | ✅ |
| بيع وشراء (Marketplace P2P) | `marketplace_screen.dart` | ✅ |
| تاكسي (حماية ديار) | `taxi_screen.dart` | ✅ |
| طرود (OTP/كود مندوب) | `parcel_screen.dart` | ✅ |
| خدمات (حلاق/طبيب…) | `services_screen.dart` | ✅ |
| وظائف | `jobs_screen.dart` | ✅ |
| التغطية والخرائط | — | ⏳ بانتظار مفتاح Google Maps |
| **✨ AI Shopping Assistant** | — | ❌ غير منفّذ (شاشة) |
| **🏭 B2B Marketplace (RFQ/جملة)** | `wholesale_screen.dart` | ⚠️ جزئي (بلا RFQ) |

**زيادات في الكود فوق التصميم** (ميزات بُنيت بعد الهاندوف): ديار Meals
(ميزانيات شركات + POS)، نظام feature-flags بالمنطقة/الجمهور، التتبّع الحي +
تعلّم EMA، ستوري نمط إنستجرام، الحلال/الكوشير والحِميات.

---

## 4) ربط الكود بالتصميم (Code Connect)

صفحة Ultra UI رقم **35 «مطابقة الكود الفعلي»** تربط كل فريم بشاشة التطبيق
(HomeScreen … GiftOrderScreen). الربط الحالي **توثيقي** (شرائح برتقالية في
Figma) لأن المشروع Flutter وأداة Figma Code Connect الرسمية React-first.
الجسر العملي المعتمد: هذا الملف + جدول i18n (صفحة 16) + رموز نظام التصميم
(صفحة 13). لا حاجة لتثبيت Code Connect الآن.

---

## 5) الفجوات المرصودة (Gaps)

> **تحديث 2026-06-13: أُغلقت الفجوات الثلاث الكبرى** (انظر القسم 8).

1. ✅ **AI Shopping Assistant** (Flagship ①): مُنفَّذ — `ai/shopping.ts`
   (`aiBuildCart`) + `AiShoppingScreen`.
2. ✅ **B2B RFQ** (Flagship ②): مُنفَّذ — `b2b/rfq.ts` + شاشات RFQ
   للتاجر والمورد + إخفاء أسعار الجملة لغير التجار.
3. ✅ **الخرائط**: مُنفَّذة ومُسيّجة بـ `kMapsEnabled` — تعمل فور ضبط مفتاح
   Google Maps الأصلي (`--dart-define=DYAR_MAPS=true`).
4. **تبويبا «اكتشف/العروض»**: مدموجان بالرئيسية (قرار مقصود) — لا إجراء مطلوب.

## 8) ما أُنجز في جولة 2026-06-13 (إغلاق الفجوات)

| الميزة | الخادم | التطبيق | التحقق |
|---|---|---|---|
| AI Shopping Assistant ① | `aiBuildCart` (Claude tool-use، تحقّق صارم من الكاتالوج) | `AiShoppingScreen` + بانر الرئيسية | tsc ✅ |
| الخرائط ② | بثّ موقع السائق (قائم) | خريطة تتبّع حية + منتقي دبوس بدقة المتر (بديل أنيق قبل المفتاح) | symbols ✅ |
| B2B RFQ ③ | `createRfq/quoteRfq/respondRfq` (عمولة خادمية) | شاشات RFQ للتاجر والمورد + قفل أسعار الجملة | tsc ✅ · 39/39 rules ✅ |

> **التبقّي للإطلاق (تشغيلي لا تصميمي):** مفتاح Google Maps الأصلي +
> تدوير المفاتيح المكشوفة + App Check + تشغيل `flutter analyze`/الاختبارات
> على بيئة فيها Flutter SDK (غير متاحة في بيئة هذه الجولة).

---

## 6) حالة التحقق التقني (هذه الجولة)

- Cloud Functions: `tsc` ✅ نظيف.
- قواعد Firestore: **34/34** اختبار ناجح (شاملة 4 اختبارات posCodes الجديدة).
- محرّك العمولات: **10/10** ناجح.
- Flutter `analyze`: غير متاح في هذه البيئة (offline) — يُشغَّل محليًا قبل الإطلاق.

---

## 7) الهدف القادم (لتحديده مع المالك)

التصميم والكود متطابقان على **نظام التصميم والخدمات الأساسية**. الرافعة
التالية ليست تصميمية بل **إغلاق الفجوتين الرائدتين + التشغيل**:

1. **بناء AI Shopping Assistant** (الميزة الأبرز تنافسيًا — لا منافس محلي
   لديه مثلها): شاشتان + دالة `aiBuildCart` تستدعي Claude.
2. **استكمال B2B**: RFQ + إخفاء أسعار الجملة + حماية العمولة.
3. **تفعيل الخرائط** فور وصول مفتاح Google Maps.
4. **تدوير المفاتيح المكشوفة** (ElevenLabs/Anthropic) قبل الإطلاق + App Check.

> اقتراح الأولوية: (1) AI Assistant ← أكبر تمايز · ثم (3) الخرائط ← أقرب
> للإطلاق · ثم (2) B2B RFQ.
